<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rehabilitation" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.drawer"
            android:label="@string/title_activity_drawer"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>
        <activity android:name=".drawer_layout" />
        <activity android:name=".FuctionOperationPage" />
        <activity android:name=".ServicesManagement" />
        <activity android:name=".PersonalDetails" />
        <activity android:name=".HomePage" />
        <activity android:name=".MainActivity" />
        <activity android:name=".PersonalList" />
        <activity android:name=".RegisterationPage" />
        <activity android:name=".LoginPage" />
        <activity android:name=".WelcomePage" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>